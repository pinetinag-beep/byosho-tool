# Python
__pycache__/
*.py[cod]
*.pyo

# 旧キャッシュ（Streamlit Cloud用データは除く）
*.parquet
!data_cache.parquet
!ward_cache.parquet
!surgery_cache.parquet

# DuckDB（バイナリ、大きいため除外）
data/*.duckdb
data/*.duckdb.wal

# 一時ファイル
_probe.xlsx
_yshiki2.xlsx

# Windows バッチファイル（ローカル専用）
*.bat

# Streamlit secrets
.streamlit/secrets.toml
