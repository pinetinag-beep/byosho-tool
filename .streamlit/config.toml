[server]
headless = true
address = "0.0.0.0"
port = 8501
maxUploadSize = 200
enableCORS = false

[theme]
# シビック・フレンドリー基調（詳細トークンは app.py のグローバルCSSで定義）
primaryColor = "#12886D"
backgroundColor = "#FAF9F6"
secondaryBackgroundColor = "#F1EFE8"
textColor = "#26251F"
font = "sans serif"

[browser]
gatherUsageStats = false
