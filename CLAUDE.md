# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## アプリ名候補

現在のリポジトリ名 `byosho-tool` は仮称。病床機能報告・DPC・届出情報・人口データなど複数の公的医療データを統合する方向性を踏まえたネーミング検討中。

方針：**親しみやすさ重視**、一般市民にも病院経営者にも届く言葉感

| 候補 | 読み | コンセプト |
|---|---|---|
| まちみる | まちみる | 「地域を見る」を一語に凝縮。ひらがなで親しみやすい |
| まちてらす | まちてらす | まち＋テラス（照らす・開放的な場所）。地域医療を照らし出すイメージ |
| まちのカルテ | まちのカルテ | 地域の医療診断書。カルテはデータが増えるほど充実するという文脈も自然 |
| ほすぴな | ほすぴな | Hospi＋な。柔らかい語感で覚えやすい |
| いりょうのわ | いりょうのわ | 医療の輪。つながり・広がりのイメージ。ロゴに◯が使いやすい |

---

## プロジェクトの目的

医療に関するデータをビジュアライズ・比較し、**医療提供体制の可視化**を実現する。

- 一般の人が地域の医療の実情を直感的に理解できるようにする
- 過剰医療提供・過疎医療提供などの問題を広く知らしめる

対象データは厚生労働省「病床機能報告」を中心に、今後他の医療データとの連携も視野に入れる。  
ユーザーは医療の専門家だけでなく、一般市民も想定する。

## ビジネス方針

**将来的な有料化を前提に開発する。**

優先ターゲット：
- **病院経営者** — 自院の競合比較・地域内ポジション把握
- **医療系コンサルタント** — 複数地域の一括分析・レポート出力
- 次点: 自治体・行政（地域医療構想策定）、医療機器/製薬メーカー営業（設備・手術件数調査）

有料化に向けた主要課題：
- 認証・ユーザー管理機能の実装
- レポート出力（PDF/Excel）
- Streamlit Community Cloudから本番インフラへの移行
- 厚労省データの商用利用可否の確認
- 特定商取引法表示・プライバシーポリシーの整備

**開発上の制約**  
率・指標・スコアは必ず公的機関（厚労省・総務省等）の定義に基づいて実装すること。  
独自解釈による指標の作成・表示は禁止。定義の出典を必ず把握した上で実装する。

### インフラ・技術スタック検討（2026年7月時点、比較検討段階・未着手）

「Streamlit Community Cloudから本番インフラへの移行」について、2つの軸で検討した内容のメモ。まだ方針決定はしておらず、実際に着手する際に再度相談してから進める。

**① ホスティング: 共用レンタルサーバー vs VPS**
- **共用レンタルサーバー（さくらのレンタルサーバ、ロリポップ等）は不可**。Streamlitは常駐Pythonプロセスとして動かす必要があるが、共用レンタルサーバーはCGI/PHP的な都度実行モデルで、常駐プロセスを許可していないため。
- 選択肢は実質**VPS**一択。2026年7月時点で調べた主要3社の2GB/4GBプラン目安（要最新確認）：
  | 事業者 | 2GBプラン | 4GBプラン |
  |---|---|---|
  | さくらのVPS | 約1,848円/月 | 約3,227〜3,740円/月 |
  | ConoHa VPS | 約980〜1,259円/月 | 約1,970〜2,408円/月（3社中最安） |
  | XServer VPS | 約990〜1,496円/月 | 約4,000円/月（4GB以上でメモリ無料1.5倍増設キャンペーンあり） |
- このアプリの現状メモリ使用量（施設基準届出約62MB＋DPC約197MB＋座標約74MB等の合計）を踏まえると、**2GBが最低ライン、4GBに余裕あり**という見立て。

**② アーキテクチャ: Streamlit継続 vs フルスクラッチ（FastAPI+React等）で作り直し**
- 有料化要件（認証・ユーザー管理・PDF出力・複数テナント対応）はStreamlitが本来想定する用途（社内向け単一セッションダッシュボード）の外側にあり、今のapp.pyには「Enterキー無効化のためJSをiframe経由で強引に注入」「st.form()がカスケード式ドロップダウンを壊す」等、Streamlitの制約を無理やり回避している箇所が複数ある。
- ただしapp.py（6,000行超）はUIロジックとビジネスロジックが一体化しており、作り直しは実質フルリライトになる。`build_*.py`群のデータパイプラインはフレームワーク非依存なので流用可能。
- 「Claudeがコードを書くので負担は軽い」という指摘は一部正しい（開発時間・機会費用は大きく減る）が、**バグ再発リスク**（年度ラベルの罠・医療機関名の表記ゆれ・様式2の年度ズレ等、実データ検証で発見した教訓を一つひとつ再検証する必要がある）と**運用の複雑化**（フロント/バックエンド分離によるAPI連携・CORS等）は、誰がコードを書くかに関わらず発生する。投資の中身は「手を動かす時間」ではなく「検証の往復回数・時間」に置き換わる。
- 結論: 現時点では時期尚早と判断し、比較検討のみで着手は保留。

---

## アプリ起動

```bash
streamlit run app.py
```

DuckDB を新規構築する場合：
```bash
python build_db.py              # 2021〜2023年 全年度（旧形式・MHLW URLからDL）
python build_db.py --years 2023 # 特定年度のみ
python build_master.py          # 医療情報ネット座標データ取り込み（初回のみ）
```

令和6年度以降の**新オープンデータ形式**を取り込む場合（`build_byosho_r7.py`）：
```bash
# 病棟票7地域（機能区分・許可病床・入院基本料）＋病院票1（医師/看護師/CT/MRI/救急）
# を医療機関コードで結合し、既存parquetに年度を追記する
python build_byosho_r7.py --dir byosho_file_R7 --year 2025 \
    --ward-glob '00171780[0-6].xlsx' --hosp 001717798.xlsx \
    --append data_cache.parquet ward_cache.parquet
```
**新形式の注意点**（2026年に令和7年度データで対応）:
- 令和6年度以降、病床機能報告のオープンデータは**ファイル分割が変わった**。病棟票（機能区分・許可病床・入院基本料・患者フロー）が7地域ファイル、病院票（医師/看護師等の人員・CT/MRI等の設備・救急車受入件数）が全国1ファイルに分かれる。旧 `build_db.py`（1ファイルに全部入り前提）は使えない。
- ただし**病棟票のセル構造（5行目ヘッダー・6行目必須任意区分・7行目からデータ）は2023年までと互換**で、既存の `load_mhlw_byosho_extended` がそのまま流用できた。`build_byosho_r7.py` は病棟票をこの関数で読み、病院票だけ列名キーワード一致で人員・設備をマッピングして医療機関コードで結合する。
- 病院票の列対応は `build_byosho_r7.py` の `_STAFF_MAP` / `_EQUIP_MAP` に定義。`救急搬送件数` は「救急車の受入件数」（年間）を採用（月別列が別にあるので完全一致で取る）。`PET`/`PETCT`/`PETMRI` は部分一致だと誤爆するので完全一致必須。
- **MHLWサーバー（www.mhlw.go.jp）は本番サーバーから403**になるため、生ExcelはユーザーがブラウザでDLして `byosho_file_R7/` に置き、gitでpushしてClaudeが処理する運用。
- `--append` は同一 `報告年度` が既にあれば置換してから追記する。列は両者の和集合で揃える。
- **4年一括ビルドは `build_byosho_all.py`**（`build_byosho_r7.build()` を各年度に適用）。現状 data_cache は**令和4〜7年度（2022〜2025）の4年分**。

**年度ラベルの落とし穴（重要・実際に事故った）**: 病床機能報告の各病棟票には「現在の機能」列（例: `保有する病棟と機能区分の選択状況（2024（令和6）年7月1日時点の機能）`）と「将来予定」列（例: `2025年7月1日の病床機能の予定`）の2つがある。**年度はファイル名やIDでなく、必ず「現在の機能」列の年で判定すること。** 旧 `build_db.py` はファイルID `001299xxx` を「2023」とラベル付けしていたが、実データの現在機能列は「2024（令和6）」で、中身は令和6年度だった（本番で「令和5年度」と誤表示していた）。2026年7月に生ファイルを取り直して現在機能列で全年度を検証し直し、正しいラベルで再構築した。ファイルID→年度の実測対応:
  - `001151xxx` = 令和4年度(2022) / `001571xxx` = 令和5年度(2023) / `001299xxx` = 令和6年度(2024) / `001717xxx` = 令和7年度(2025)

## Git・デプロイ

**Streamlit Cloud（`byosho-tool-testver.streamlit.app`）は `master` ブランチを参照している。GitHub上のブランチは`master`（本番）のみが正式運用。他に一時的な作業ブランチ（`claude/xxx`等）が残っていても、それはmasterにマージ済みの残骸か、他セッションが作業中の一時ブランチであり、"pushすべき2つ目の本番ブランチ"では絶対にない。**

**過去に`main`という名前のローカルブランチ・リモートブランチが存在した名残で、「masterとmainの両方にpushする」という誤った運用がCLAUDE.mdに書かれていた時期があった（2026年7月に発覚・修正）。`main`はGitHub上には存在しない（`gh repo view --json defaultBranchRef`で確認可能）。ローカルの`git branch -a`が`origin/main`を表示する場合、それは削除されたリモートブランチの古いキャッシュ（stale remote-tracking ref）であり実体はない。** この思い込みにより、実際には別環境（別のClaude Codeセッション等）がmasterに直接pushした変更にローカルが気づかず、21コミット分の乖離に気づかないまま作業を進めてpushが`rejected`になる事故が起きた。

**恒久対応（2026年7月実施）**: ローカルブランチ名を`master`に統一し、`origin/master`を追跡するよう設定済み（`git branch --set-upstream-to=origin/master master`）。これにより：
- `git status` だけで本番との乖離（ahead/behind）が正確に分かる（以前のように存在しない`origin/main`と比較して誤った情報が出ることはない）
- pushは単に `git push` でよい（`HEAD:master`等のrefspec指定は不要）
- pullも単に `git pull` でよい

**作業を始める前に必ず`git pull`（またはfetch状況を`git status`で確認）してから着手すること。** 他環境からの直接pushでローカルが本番より古くなっていることがあるため、これを毎回の作業の第一歩とする（気づいた時にfetchするのではなく、着手前に必ず行う）。乖離があれば`git rebase origin/master`で取り込み、pushの前に必ずローカルでStreamlitを起動して主要画面の動作を再確認する（UI仕様が変わっている可能性があるため）。

もし別のClaude Codeセッションが独自の作業ブランチ（`claude/xxx`等）を作ってpushした場合は、masterにマージ後すみやかにそのリモートブランチを削除する（`git push origin --delete <branch>`）。マージ済みブランチを残しておくと「pushすべきブランチが複数ある」という誤解が再発する温床になる。

## データ管理

- **ローカル**: `data/byosho.duckdb`（`.gitignore` 対象）
- **Streamlit Cloud**: `data_cache.parquet` / `ward_cache.parquet` / `surgery_cache.parquet`（リポジトリに含める）
- DuckDB を更新した場合は parquet も再生成してコミットする：
  ```python
  import duckdb, pandas as pd
  con = duckdb.connect('data/byosho.duckdb')
  con.execute('SELECT * FROM hospitals').fetchdf().to_parquet('data_cache.parquet', index=False)
  con.execute('SELECT * FROM wards').fetchdf().to_parquet('ward_cache.parquet', index=False)
  con.execute('SELECT * FROM surgery').fetchdf().to_parquet('surgery_cache.parquet', index=False)
  con.close()
  ```
- MHLW サーバー（`www.mhlw.go.jp`）は本番サーバーから 403 になる。ダウンロードはユーザーのブラウザから行う。

## アーキテクチャ

```
app.py          ← Streamlit UI（全タブ・サイドバー）
  ├── data_processor.py  ← データ読み込み・集計ロジック
  ├── charts.py          ← Plotly グラフ定義
  ├── geocoder.py        ← 座標取得（locations → geocache → Nominatim の優先順）
  ├── sample_data.py     ← デモ用サンプルデータ生成
  ├── build_dpc.py       ← DPC調査データの取り込み（DuckDB経由・dpc_*.parquet を生成）
  ├── build_dpc_append.py ← DPC追加年度をparquetに直接追記（本番はDuckDB無しのため）
  ├── build_shisetsu_kijun.py ← 施設基準届出Excelの取り込み（下記セクション参照）
  └── build_byosho_r7.py ← 令和6年度以降の新形式 病床機能報告の取り込み（上記「アプリ起動」参照）

shisetsu_search.py        ← 施設基準届出「検索専用」の独立アプリ（本体と別デプロイ可能）
parse_chubu_shisetsu_pdf.py ← 施設基準届出「届出受理医療機関名簿」PDF形式の専用パーサー
```

起動時のデータ読み込み順：
1. `data/byosho.duckdb` が存在すれば DuckDB から読む（`st.cache_data`）
2. なければ `data_cache.parquet` 等から読む（Streamlit Cloud 用）
3. どちらもなければ「データが未準備です」→ サンプル生成 or Excel アップロード

## data_processor.py 主要関数

| 関数 | 説明 |
|---|---|
| `load_mhlw_byosho_extended(bytes, year)` | 様式1 Excel → (hospitals_df, wards_df) |
| `load_multiple_mhlw_extended(list, year)` | 複数地域ファイルを結合（byosho は7〜8地域ファイル） |
| `load_mhlw_yoshiki2(bytes, year)` | 様式2 Excel → 手術集計 df |
| `load_mhlw_shisetsu(bytes)` → `merge_shisetsu()` | 設備票を hospitals に結合 |
| `add_derived_columns(df)` | 稼働率・医師/看護師 per100床・CT台数合計などを付加 |
| `load_hospitals_from_db / load_wards_from_db / load_surgery_from_db` | DuckDB → DataFrame |

## 様式2（手術データ）の注意点

- **2021年は7地域ファイル**（000953885〜000953892.xlsx、891 は欠番）。2022/2023年は全国1ファイル。
- 2021年の Excel は5段組ヘッダー（rows 0〜4）。`_detect_yoshiki2_is_multilevel()` で自動判定。
- 2021年の「二次医療圏名」列は `構想区域名` という列名で入っている場合がある（`load_mhlw_yoshiki2` 内で吸収済み）。

## DB スキーマ（概要）

- `hospitals`: 病院単位・年度別（20,830行 / 2021〜2023）
- `wards`: 病棟単位（81,687行）
- `surgery`: 手術実績、病院単位・年度別（12,320行 / 2021: 5,419・2022: 3,407・2023: 3,494）
- `geocache`: Nominatim 取得済み座標キャッシュ
- `locations`: 厚労省公式座標（`build_master.py` 実行後）

## charts.py 主要関数

`ranking_table_fig(df, highlight, rank_col, show_cols, col_labels)` — ランキングタブで使用。`rank_col` で並び替え基準（許可病床数・稼働率・医師数など7種）を切り替える。

## app.py 構造（行番号目安）

| 範囲 | 内容 |
|---|---|
| 1〜370 | import・定数・キャッシュ関数 |
| 371〜465 | DuckDB / parquet からのデータ読み込み・セッションステート初期化 |
| 466〜700 | サイドバー（データ未準備時のアップロード UI・病院選択アコーディオン） |
| 703〜780 | 管理者セクション（キャッシュクリア・様式2アップロード・ダウンロード） |
| 781〜 | メインエリア（tab1〜tab7） |

## セッションステート 主要キー

| キー | 内容 |
|---|---|
| `df` | 病院データ DataFrame（全年度） |
| `ward_df` | 病棟データ DataFrame |
| `surgery_df` | 手術データ DataFrame |
| `_datasrc` | `"db"` / `"parquet"` / `"sample"` / `"none"` |
| `_sb_open` | サイドバーで開いているアコーディオンセクション（`"①"` 等） |
| `_view_mode` | `"detail"`（病院詳細）/ `"search"`（条件検索） |
| `_yoshiki2_parquet` | 管理者パネルで様式2インポート後の parquet バイト列（ダウンロード用） |

## ユーザーへの作業依頼ルール

ユーザーのWindows環境での作業が必要な場合は、必ず以下の形式でステップ・バイ・ステップで指示する。まとめて1行で言わない。

**ユーザー環境の注意点：**
- Pythonの実行は `py` コマンドを使う（`python` はMicrosoft Store stubで動かない）
- 作業フォルダ: `C:\Users\inter\OneDrive\Desktop\byosho_tool`

**指示の例（住所データを追加する場合）：**

1. コマンドプロンプトを開く
2. 以下を実行してフォルダに移動する：
   ```
   cd C:\Users\inter\OneDrive\Desktop\byosho_tool
   ```
3. 以下を実行して住所データを取り込む：
   ```
   py build_master.py
   ```
4. 完了したら、以下を実行してparquetをエクスポートする：
   ```
   py -c "import duckdb, pandas as pd; con=duckdb.connect('data/byosho.duckdb'); con.execute('SELECT * FROM hospitals').fetchdf().to_parquet('data_cache_new.parquet', index=False); con.close(); print('done')"
   ```
5. `data_cache_new.parquet` をこのチャットに添付する
6. Claudeがコミット・プッシュする

---

## データ更新の全般的な流れ

DuckDBを更新してStreamlit Cloudに反映する一般手順：

**ステップ1（ユーザー：Windowsで実行）**
1. コマンドプロンプトを開く
2. `cd C:\Users\inter\OneDrive\Desktop\byosho_tool` で移動
3. 必要なスクリプトを `py <スクリプト名>` で実行

**ステップ2（ユーザー：parquetをエクスポート）**
4. 以下を実行して parquet を生成：
   ```
   py -c "import duckdb, pandas as pd; con=duckdb.connect('data/byosho.duckdb'); con.execute('SELECT * FROM hospitals').fetchdf().to_parquet('data_cache.parquet', index=False); con.execute('SELECT * FROM wards').fetchdf().to_parquet('ward_cache.parquet', index=False); con.execute('SELECT * FROM surgery').fetchdf().to_parquet('surgery_cache.parquet', index=False); con.close(); print('done')"
   ```
5. 生成された parquet ファイルをこのチャットに添付する

**ステップ3（Claude：コミット・プッシュ）**
6. Claude がリポジトリに配置してコミット・プッシュする

---

## 手術データの更新手順

手術データ（様式2）を更新する場合：

1. アプリの管理者パネルで様式2ファイルをアップロード → 「手術データを取り込む」
2. 成功後に表示されるダウンロードボタンで `surgery_cache.parquet` を取得
3. このチャットに `surgery_cache.parquet` を添付する
4. Claude がリポジトリにコミットして Streamlit Cloud に反映する

**2021年は7地域ファイル**（000953885〜000953892.xlsx、891は欠番）を全て選択してアップロードすること。

### 令和6年度以降：byosho_file_R4〜R7フォルダに様式2が紛れ込んでいる

`build_byosho_r7.py`／`build_byosho_all.py` は `byosho_file_R4`〜`R7` フォルダ内のファイルのうち、病棟票7〜8ファイル＋病院票1ファイルしか使っていない。だが実際にはユーザーが各フォルダに **病棟票と同じ件数分の様式2（手術データ）ファイルも一緒にダウンロードして入れていた**（2026年7月発覚）。列見出しに「手術総数」「臓器別の状況」等があり、既存の `load_mhlw_yoshiki2()` がそのまま使える（列構造が2022/2023年の単一ヘッダー形式と互換）。

**報告年度に関する重大な罠**: 様式2ファイルの実績期間はフォルダ名（＝病棟票の報告年度）より**1年古い**。病床機能報告は7月1日時点のward census（例: `byosho_file_R7` = 令和7年7月1日時点の病棟情報）と、直近に確定している**前年度1年分**の手術実績をセットで提出する仕組みのため。実例: `byosho_file_R7` 内の様式2ファイルの見出しには「【令和６年４月から令和７年３月診療分】」とあり、これは令和6年度（2024年度）の実績＝報告年度は2025ではなく **2024**。この年度ズレに気づかず「R7フォルダ＝2025年度」と単純にラベル付けして取り込んだ結果、件数が既存の2023年度データとほぼ一致する（2023: 8,680行 vs 誤ラベル時の値8,681行）という形で発覚した——**新規データのはずの行数が既存の別年度と酷似していたら、年度ラベルの取り違えを疑うこと**（病床機能報告本体で過去に起きた「年度ラベルの落とし穴」と同じ事故パターン）。

`build_yoshiki2_from_byosho.py` は、ファイル埋め込みの「令和X年Y月から令和(X+1)年Z月診療分」というテキストから報告年度を自動判定して取り込むため、この種の年度取り違えを防げる：
```bash
python build_yoshiki2_from_byosho.py byosho_file_R7 \
    --exclude 001717798 001717800 001717801 001717802 001717803 001717804 001717805 001717806
```
`--exclude` には様式1（病棟票・病院票）で既に使用済みのファイルIDを指定する（`build_byosho_all.py` の `JOBS` を参照）。2026年7月時点で `byosho_file_R7` の様式2（令和6年度＝2024年度実績、8,615病院）を取り込み済み。`byosho_file_R4`〜`R6` の様式2（令和3〜5年度実績）は取り込み時に既存の2021〜2023年度データとほぼ同一件数だったため、既存データを優先し取り込みを見送った。令和7年度（2025年度）の手術実績は、令和8年度の病床機能報告オープンデータ（`byosho_file_R8` 相当）が公表されるまで入手できない。

## 施設基準届出データ（診療報酬）

各地方厚生局が公表する「施設基準届出受理状況」を全国分収集し、`shisetsu_kijun_cache.parquet` として保持している。病院詳細ページの「施設基準届出」セクションと、条件検索の「施設基準届出」タブ、および独立アプリ `shisetsu_search.py` で使用する。

### データの二重構造（重要）

地方厚生局が公表するファイルには**2つの全く異なる系統**がある。どちらも「届出受理医療機関一覧表／名簿」という似た名前で紛らわしいので注意。

| 系統 | 内容 | 対応スクリプト |
|---|---|---|
| **全件スナップショット**（Excel/PDF） | ある時点の届出状況を病院・届出単位で全件記載 | `build_shisetsu_kijun.py`（Excel） / `parse_chubu_shisetsu_pdf.py`（PDF） |
| **新規・変更届出のみ**（PDF diff） | 直近数週間の新規／変更分のみを記載する差分レポート | `parse_shinki_pdf.py`（scratchpad、本採用せず） |

**必ず「全件スナップショット」形式のファイルを使うこと。** 差分レポートは一部の医療機関しか載っておらず、全体を代替できない。

### Excelの内部構造（`build_shisetsu_kijun.py`）

全件スナップショットのExcelは1つの届出が**複数行**に展開される構造になっている：

```
行1: 医療機関番号・医療機関名称・受理届出名称・受理記号・受理番号・算定開始年月日（本体行、備考列は空）
行2以降: 同じ届出について「備考（見出し）」「備考（データ）」列に
         病棟種別・病床区分・病棟数・病床数・区分 等の内訳が1行ずつ入る
```

`_normalize_label()` で見出しの表記ゆれ（全角スペース・「届出に係る」プレフィックス等）を吸収し、既知ラベル（病棟種別／病床区分／病棟数／病床数／区分）は専用列に、それ以外は `内訳その他` にまとめる。

**重要：地方厚生局によって「備考」欄を埋めているかどうかが全く違う。** 同じ24列Excelフォーマットでも、実際にデータが入っているかは厚生局の運用次第。実測した状況（2026年時点）：

| 厚生局 | 区分（備考詳細）の有無 |
|---|---|
| 東北・九州（+沖縄）・東海北陸・四国 | ✅ あり |
| 関東信越・近畿・北海道・中国 | ❌ 備考が常に空 |

四国4県（香川・徳島・愛媛・高知）は当初「コード内容別医療機関一覧表」という**別物のファイル**（開設者・診療科・病床数等の医療機関名簿で、届出情報自体が無い）しか配布されておらず使えなかった（2026年7月に一度再取得を試みたが、その時も同じ誤った形式だった）。**2026年7月、正しい「届出受理医療機関名簿」形式（24列フォーマット）が入手でき、全国47都道府県すべてが揃った。** 四国は有床診療所の入院基本料区分（`有床診療所入院基本料`等）が特に充実しており、病床機能報告でカバーできない有床診療所区分の貴重な追加データ源になっている。

### PDFパーサー（`parse_chubu_shisetsu_pdf.py`）— 2026年7月以降は基本不要

東海北陸厚生局（富山・石川・岐阜・愛知・静岡・三重）と茨城・兵庫は、当初PDF（「届出受理医療機関名簿」）でしか届出情報が手に入らなかったため専用パーサーを組んだが、**2026年7月に該当8都道府県すべてで正規のExcel（24列フォーマット）が入手できることが確認された**。以後の更新はExcelを`build_shisetsu_kijun.py`（`--dir shisetsu_raw/`）に通すだけで済み、このPDFパーサーは基本的に不要。ただし今後別の厚生局がPDFしか公表しないケースに備えて残してある。以下は当時の教訓（ただし **受理届出名称（正式名称）が無く、受理記号（略号）と算定期間のみ**が記載されるため、`build_kigou_to_meisho()` で既存の `shisetsu_kijun_cache.parquet` から「受理記号→受理届出名称」の逆引き辞書を作り、正式名称を復元していた）。

PDF解析で得た教訓（同種のPDFを今後解析する際に必ず踏まえること）：

1. **1医療機関のレコードが複数ページにまたがる**（大病院は届出数が多く数ページ分続く）。ページ単位で処理すると境界を誤検出するため、`page.extract_words()` の `doctop`（文書全体でのY座標。`top` はページ内相対値なので使えない）で全ページを結合してから処理する。
2. **医療機関番号アンカーの検出**は「項番＋医療機関番号がスペース無しで連結される」ため（例："13901,1802,6" = 項番139 + 医療機関番号01,1802,6）、行頭マッチ（`^`）ではなく末尾からの `search()` で検出する。区切り文字も厚生局によって `-` `,` `・` とバラバラ。
3. **各ページ冒頭のヘッダー行**（タイトル・列見出し・「電話番号（FAX番号）」等）を除外しないと、複数ページにまたがるレコードの途中にヘッダーテキストが混入する（`top < 96` を除外）。
4. **性能**: 医療機関数×総単語数の総当たりスキャンは病院数の多い県（愛知3,594件等）でO(n²)的に遅くなる。`doctop` でソート済みリストに対し `bisect` で区間を取得すること。
5. 「（受理記号）第N号」の確定行に、直前の病床数等の数値が結合されるケースがある（例："378（医療ＤＸ）第494号"）。正規表現は `^` アンカーを付けず `search()` で検出する。

### 区分（急性期一般入院料等）の全国カバレッジ問題と対処

施設基準届出データだけでは、**一般病棟・療養病棟・障害者施設等の区分（段階）が全国的には揃わない**（上表の通り関東信越・近畿・北海道・中国四国は空）。

**解決策**: 病床機能報告データ（`ward_cache.parquet` の `入院基本料` 列）は、病床を持つ医療機関が毎年義務的に報告する全国統一フォーマットであり、急性期一般入院料１〜７・地域一般入院料１〜３・療養病棟入院料１・２・障害者施設等入院基本料の区分を**全国100%**保持している。そのため `app.py` では、施設基準届出側の区分が空の場合に `ward_df` から自動補完している（病院詳細ページ・条件検索の両方）。

ただし **精神病棟・有床診療所・結核病棟は病床機能報告の対象外制度**のため、この3種は地方厚生局データ（東北・九州・東海北陸のみ）に頼るしかない。

### 施設基準届出データを追加・更新する際の注意

- Excel/PDFは容量が大きいため、チャット添付ではなく `shisetsu_raw/` フォルダに置いて `git push` する運用にしている（ユーザー側でWindowsのgitブランチが `main` 等になっている場合、`git push origin HEAD:claude/work-status-check-8PEo3` の形で明示的にpushする必要がある）
- 都道府県ごとにマージする際は、**列を追加する新データフレームの列に合わせて既存データフレーム側を絞り込むと、既存データにあった列（`医療機関名_正規化` 等）が消える事故が起きる**（実際に発生し本番でKeyErrorになった）。必ず「両者の列集合の和集合」で揃えてからconcatする。
- 条件検索の区分フィルターUIは、親の届出名称チェックボックスの選択と離れた場所に置くと「気づかれない」（実際に指摘された）。`st.container(border=True)` で視覚的に区別し、選択した届出名称の**直下**に表示すること。
- `shisetsu_raw/` を丸ごと入れ替えて全件再構築する場合は `python build_shisetsu_kijun.py --dir shisetsu_raw/` を使うと楽（フォルダ内の全`.xlsx`を自動処理し、24列フォーマットに合わないファイル—四国4県の「コード内容別医療機関一覧表」等—は自動でスキップされ、エラーにはならない）。ただしこの`--dir`実行は`shisetsu_kijun_cache.parquet`を**丸ごと上書き**するため、実行前に必ずバックアップを取り、処理後は「スキップ（データなし）」と表示された都道府県がないか・行数が不自然に減っていないかを旧データと比較してから本番に反映すること。
- `shisetsu_raw/届出受理医療機関名簿の受理番号欄における略称一覧.pdf`（厚労省公表の略称→正式名称対応表、2026年7月にユーザーが追加）は、全都道府県がExcel化されたことで通常の処理では不要になったが、`受理記号`列の意味を調べたい時や将来また略称のみのソースが出てきた時のための参考資料としてリポジトリに保持している。

### 差分（新規・変更／失効）PDFによる最新化（全件再構築を伴わない更新）

`build_shisetsu_kijun.py --dir` は全件スナップショットの丸ごと再構築用だが、より軽量に**直近の増減分だけ**を反映したい場合は、地方厚生局が公表する「届出受理医療機関一覧表」（新規・変更届出のみ）「保険医療機関の届出関係失効一覧表」（失効のみ）という2種類の**差分PDF**を使う（例フォルダ: `shisetsu_new_20260712/`、ファイル名は「`<都道府県> 新規.pdf`」「`<都道府県> 失効.pdf`」）。処理は3ファイル構成：

- `parse_shisetsu_shinki_pdf.py`：新規・変更PDFをパース。全件スナップショットと違い**受理届出名称（正式名称）がそのまま載っている**ため受理記号→正式名称の逆引きは不要。
- `parse_shisetsu_shikkou_pdf.py`：失効PDFをパース。正式名称は載っておらず受理記号のみのため、既存データからの**行削除専用**に使う（都道府県名＋医療機関番号＋受理記号のキーで一致行を消す）。
- `apply_shisetsu_diff.py <フォルダ>`：上記2つを都道府県ペアで実行し、失効→削除、新規・変更→（同キー削除後）追加の順で `shisetsu_kijun_cache.parquet` に適用する。適用前に `.bak` バックアップを自動作成し、施設種別を再判定する。

**この差分PDF形式のパースで実際に踏んだ落とし穴（今後また似た形式が来た時のために記録）**：

1. **医療機関番号の区切り文字が都道府県ごとに全くバラバラ**（`010,674,9` `01-0308-5` `010,006.5`（カンマとピリオド混在）`01・1430・3`、さらに `011 781.5` のように**空白で2トークンに分割される県すらある**）。個々のトークンに対する正規表現一致では対応しきれないため、`_find_anchors()` は行全体（同じdoctopの単語群）を連結してから数字以外を除去し、**ちょうど7桁になるか**でアンカー行を確定する方式に変更した。桁のグループ化を信用してはいけない。
2. 失効PDFには「項番」列と「医療機関番号」列が両方とも同じ `x0<100` の狭い領域にあり、行ごと結合すると項番が医療機関番号の頭に連結されてしまう（例：項番1+番号"01,1342,0"→"101,1342,0"）。医療機関番号の抽出には項番列を除いた `x0` 下限（58等）を使うこと。
3. **都道府県名の解決ミスで一度マージが実質無効化する事故を起こした**：フォルダ内のファイル名（例「岡山 新規.pdf」）から取り出した短縮名「岡山」をそのままパーサーへの都道府県名ヒントとして渡してしまい、出力の都道府県名が既存データの正式名称「岡山県」と一致せず、削除・上書きのキー照合がほぼ働かないまま108,755行が別名義で積み上がった（適用前916,894行→削除わずか73行→適用後1,025,576行、という異常値で発覚）。`.bak` から即座にロールバックし、ファイル名からの都道府県名解決は必ず `_guess_pref_from_filename()`（正式名称の辞書引き）を経由するよう修正して再実行した（適用後は削除62,079行・追加108,755行で整合）。**マージ系スクリプトを書いたら、適用前後の行数だけでなく「削除件数が桁違いに少なくないか」も必ず確認すること。**
4. 失効PDFは受理番号（第N号）を含まないため、同一医療機関が同じ受理記号で複数回登録している場合（例：同じ加算を2回に分けて届出）にどちらが失効対象か区別できない、という**データ側の構造的な限界**がある。実害は僅少（重複キー行の割合は適用前後で0.47%→0.44%とほぼ不変）だが、完全な一意性は保証できない。
5. 適用後の検証は「行数の増減」だけでなく、具体的な1医療機関を追跡して**期待した届出が実際に追加/削除されているか**まで確認すること（今回は岡山県河田病院・北海道札幌中央病院で実施）。

### 施設種別（病院／有床診療所／無床診療所）の判定

`build_shisetsu_kijun.py` の `_classify_facility_types()` で、医療機関ごとに届出済みの入院基本料の種類から判定し、`施設種別` 列として `shisetsu_kijun_cache.parquet` に保持している：

- 一般病棟／療養病棟／精神病棟／障害者施設等／結核病棟／特定機能病院／専門病院の入院基本料のいずれかを届出 → **病院**
- 有床診療所入院基本料（または療養病床版）を届出 → **有床診療所**
- 入院基本料の届出が一切ない → **無床診療所**（入院を算定するには何らかの入院基本料の届出が必須なため、消去法での判定だが実質確実）

本番データで3種の重複（矛盾ケース）は0件だった。

**重要：既存の病院検索・病院詳細ページ（病床機能報告ベース）は実質「病院」しか対象にしていない。** `data_cache.parquet`（病床機能報告）と施設基準届出データの名称を突き合わせたところ、施設基準届出データ側の「有床診療所」3,916件のうち病床機能報告と一致したのはわずか1件だった（病院7,002件は5,270件が一致）。そのため「地域から選ぶ」「病院名で探す」等の既存の検索経路では有床診療所・無床診療所はほぼ見つからない。これを踏まえ、施設基準届出データを直接ベースにした専用の検索ページ `_view_mode == "clinic_search"`（🩺診療所を探す）を新設した。都道府県・施設種別・医療機関名・届出名称キーワードで絞り込める。表示件数が多い場合、届出項目一覧のgroupbyを表示対象分だけに絞ってから行うこと（全件に対して行うと非常に遅くなる。実際にこれで無限に近い待ち時間になるバグを起こした）。

### 標榜科目（診療科）データは未収録

このデータのどこにも標榜科目（内科・外科等）は入っていない。四国4県で誤って取得した「コード内容別医療機関一覧表」（施設基準届出には使えない別物のファイル）には、実は標榜科目の略記がそのまま入っていることを確認した（例: 「内　小　外」＝内科・小児科・外科）。ただしこの形式は四国4県でしか入手できておらず全国データにはならない。全国規模で取得できる可能性が最も高いのは「医療情報ネット オープンデータ」（`build_master.py` が座標抽出に使っているe-Gov ZIPと同じデータソース。制度上、患者が診療科目で医療機関を検索できることを目的にしているため）だが、ZIP内の座標以外のCSV列は未調査（本番サーバーからは `data.e-gov.go.jp` へのアクセスがプロキシで403になるため、ユーザーのブラウザでダウンロードして中身を確認する必要がある）。

## 学会認定施設データ

各学会（外科学会・内科学会等）が公表する「専門医研修施設」「認定施設」等の一覧を取り込み、既存の病院データと医療機関名で突合して、条件検索で「◯◯学会認定施設で絞り込む」ができるようにする機能。2026年7月、内科学会・内分泌学会・眼科学会・整形外科学会・消化器外科学会・神経学会・産婦人科学会の7学会分で実装。

- **生データの置き場**: `gakkai_nintei_raw/<学会名>/`（ユーザーが学会ごとにファイルを追加していく運用）
- **学会ごとにPDF形式が全く異なる（重要）**。新しい学会を追加する際は、既存の抽出関数が流用できると思い込まず、必ず該当PDFの構造を先に確認すること。
  - **内科学会**（`gakkai_nintei_raw/内科学会/`）: 基幹施設・連携施設・特別連携施設の3区分がそれぞれ別PDF（`kikan_2026.pdf` / `renkei_2026.pdf` / `t_renkei_2026.pdf`）。ページ単位で「都道府県／施設名」の2列表が並ぶだけのシンプルな構造で、`pdfplumber` の `extract_tables()` がそのまま使える。
  - **内分泌学会**（`gakkai_nintei_raw/内分泌学会/`）: 支部ごとに分かれた5ファイル（`shisetsu_1〜5.pdf`）。「都道府県／施設名+診療科名（1セルに結合）／郵便番号／住所」の4列表。施設名と診療科名がスペース区切りで1セルに同居しているため、セルを空白分割し**最後のトークンを診療科名として除去**する処理が必要（診療科名は必ず「科」で終わる1トークンという経験則、494行全件で例外なし）。同一施設が複数診療科で重複登場するため、学会×区分×施設名で重複除去してから使う。
  - **眼科学会**（`gakkai_nintei_raw/眼科学会/`）: 「一般研修施設.pdf」（870件）は「県名／研修施設番号／研修施設名称」の3列表で抽出は容易だが、**県名が「青森」のようにサフィックス（都/道/府/県）省略形で記載されている**ため、`_normalize_pref_suffix()` で正式名称に変換してから突合する必要がある（これをせずに都道府県名で突合すると、北海道以外ほぼ全滅する）。「基幹研修施設.pdf」（109件）は**画像として保存されたPDF（テキスト層なし、`extract_text()`が空になる）でPDFからは抽出不可**だったため、学会公式ページ（`/senmon/purpose/kensyupro.html`）のHTML表を`kikan_shisetsu.html`として保存し、`_extract_kikan_html()`（BeautifulSoup使用）でパースする代替手段を採用（PDFがダメでも同じ情報が別ページにHTML表で載っていないか確認する価値がある好例）。
  - **消化器病学会**（`gakkai_nintei_raw/消化器病学会/`）: 「認定施設指導医一覧」という全く別物の形式（96ページ）。1施設＝主要施設コード＋指導医リスト＋関連施設コード群という複雑な複数カラムレイアウトで、`extract_text()`では列が入り交じって読み取れない。**未対応**（対応するなら施設基準届出PDFパーサーと同様、`extract_words()`のx0座標で列を分離する必要がある。対応時期未定）。
  - **整形外科学会・消化器外科学会**（Excel形式）: いずれも「都道府県／施設名」列を持つシート（整形外科学会は「全施設一覧」シート、消化器外科学会は「認定施設一覧」シート）を持つシンプルな構造で、`pandas.read_excel()`で直接読める。区分は単一（全件同じカテゴリ）。
  - **神経学会**（Excel、「全国一覧」シート）: 都道府県・施設名に加え、行ごとに異なる「施設区分」列（教育施設／准教育施設／教育関連施設）を持つため、抽出関数は`(都道府県, 施設名, 区分)`の3要素タプルを返す（`_SOCIETY_CONFIG`の抽出関数は2要素または3要素タプルのどちらでも返せる設計）。
  - **産婦人科学会**（Excel、「専門研修施設一覧」シート）: 施設区分（基幹／連携）は行ごとに異なるが、**都道府県列が存在しない**（施設名・施設区分のみ）。都道府県が分からないため、通常の「都道府県内でのサフィックス一致」フォールバックは使えず、`_build_hospital_lookup()`が別途構築する全国版完全一致辞書（正規化施設名→医療機関コードの集合）に対して、**候補が一意に定まる場合のみ**マッチさせる（同名施設が複数都道府県にまたがる場合は曖昧としてマッチさせない、誤マッチ防止のため意図的に緩い基準にしている）。
- **ビルドスクリプト**: `build_gakkai_nintei.py`。`_SOCIETY_CONFIG` 辞書に学会名→`{"extractor": 抽出関数, "files": [(ファイル名, 区分ラベル), ...]}` を追記していく方式（学会ごとに形式が違うため、抽出関数も学会・ファイルごとに使い分ける設計。ファイルタプルに3つ目の要素を足すとその1ファイルだけ抽出関数を上書きできる）。実行すると `gakkai_nintei_cache.parquet` を再生成する。`beautifulsoup4`（HTML抽出用）が必要（`pip install beautifulsoup4`、requirements.txtには未追加＝pdfplumberと同様デプロイ対象外のオフライン処理用スクリプト専用の依存関係という扱い）。
- **医療機関名の突合ロジック**（`_normalize()` + `_match()`）: 病床機能報告データ（`data_cache.parquet`、全年度分の表記ゆれを含めて逆引き辞書化）に対し、①法人格プレフィックス除去＋全角正規化した完全一致、②同一都道府県内でのサフィックス一致（学会データが法人名を省略している/病床機能報告側が法人名を含む、等の片側だけ付加語があるケースを吸収。4文字未満の短い名称同士は誤マッチ防止のため対象外）の2段階でマッチングする。都道府県が分からないデータ（産婦人科学会）だけは③全国版完全一致（一意に定まる場合のみ）にフォールバックする。実測一致率は内科学会78.0%（基幹施設のみなら97.6%、特別連携施設は56.5%——多くは病床機能報告の対象外の無床診療所等のため一致しないのが正常）、内分泌学会92.3%、眼科学会89.9〜93.9%、整形外科学会96.0%、消化器外科学会89.7%、神経学会92.9%、産婦人科学会63.1%（都道府県で絞れずサフィックス一致も使えないため他学会より低め。未一致の多くは病床機能報告の対象外と見られる有床診療所規模の産科クリニック）。**東京慈恵会医科大学附属病院（本院）は複数学会で一致しない**ことを確認済みで、ユーザーからも「病床機能報告に記載がなく恒久的に突合できない」と確認が取れている（＝突合ロジックの不具合ではなく、この施設が`data_cache.parquet`に元々存在しない恒久的なデータ欠落。今後別の学会を追加した際にも同じ病院が未一致リストに出てきたら、この既知の欠落だと判断してよい）。
- **都道府県名の表記ゆれに注意**: 学会によって「青森県」でなく「青森」のようにサフィックス省略形で記載されることがある（眼科学会で発覚）。`_match()` は都道府県名の完全一致を前提にしているため、新しい学会を追加する際は抽出した都道府県名の一覧を必ず確認し、`data_cache.parquet` 側の正式名称（都道府県名は全て都/道/府/県付き）と揃っているか検証すること。
- **`pd.DataFrame()`化すると`None`が`NaN`（float）に化ける罠**（産婦人科学会追加時に発覚・実際に事故った）: 都道府県列を持たない産婦人科学会の抽出関数は`pref=None`を返す設計だったが、`pd.DataFrame(all_rows)`で全学会分の行を1つのDataFrameにまとめると、同じ「都道府県名」列に他学会の実在の都道府県文字列も混在するため、pandasが列の型を統一しようとして`None`を**暗黙に`NaN`（float型）へ変換**してしまう。その結果`_match()`内の`if pref is None:`という同一性チェックが一度もTrueにならず、産婦人科学会だけ一致率が0.0%になる形で発覚した（他学会は文字列のprefを持つため影響なし）。デバッグは「関数を直接呼べば正しく動く」→「実際にDataFrameへ格納された値の型を`type()`で確認する」という手順で原因を特定した。**修正**は`pref is None or (isinstance(pref, float) and pd.isna(pref))`のように`NaN`も明示的に拾うこと。教訓: DataFrame化を挟むと`None`はもう`None`のままとは限らない、`is None`ではなく`pd.isna()`を使うのが安全（他の`*`マスク値やcategory dtype同様、「データ型がpandasを経由すると変わりうる」系の教訓の一つ）。
- **保存先スキーマ**（`gakkai_nintei_cache.parquet`）: `学会名` / `区分` / `都道府県名` / `施設名` / `施設名_正規化` / `医療機関コード`（マッチできた場合のみ値あり）/ `マッチ状態`（"一致"/"未一致"）。1施設1行ではなく「1施設×1区分＝1行」の縦持ち。
- **UI**: 条件検索（`_view_mode == "search"`）の絞り込みタブに「🎓 学会認定施設」を追加（`_tab_gakkai`）。フィルタリングは `医療機関コード` で直接絞り込む（施設基準届出フィルターのような`.apply()`での名称照合は不要、突合済みのコードを使うだけなので高速）。
  - **表示は「学会ごとに1行」ではなく「学会名－区分」を1つのラベルにまとめた単一マルチセレクト**（2026年7月、学会が増えると縦に大量に場所を取ると指摘され変更）。Streamlitのマルチセレクトは入力で選択肢を絞り込めるため、「内科学会」等と入力すればその学会の区分だけに絞れる。今後何十学会に増えても表示領域が増えない設計。学会ごとに独立したウィジェットを並べる実装に戻さないこと。

## 新地域医療構想策定ガイドライン（厚生労働省、令和8年7月公表）への対応

2026年7月、厚労省が2040年を見据えた新しい「地域医療構想策定ガイドライン」を公表した
（https://www.mhlw.go.jp/content/10800000/001718620.pdf 。本番サーバー・WebFetchいずれからも403で直接取得不可、内容はニュース記事・検討会資料の検索結果から把握）。
`_view_mode == "region_vision"`（地域医療構想を分析）の記載・試算をこれに合わせて更新した。

**新ガイドラインの主なポイント**:
- **医療機関機能報告**（令和8年10月開始予定・病院単位の新しい報告制度）を創設。構想区域レベルの4機能「高齢者救急・地域急性期機能」「在宅医療連携機能」「急性期拠点機能」「専門等機能」＋広域レベルの機能（医育及び広域診療機能）を医療機関自身が申告する。**「高齢者救急・地域急性期機能」は1つの機能として定義されており**、1病院が「急性期拠点機能」と同時に持つことは認めない、という制約がある。
- 病床機能報告自体（高度急性期・急性期・回復期・慢性期の4区分、病棟単位）は存続するが、**「回復期」は令和9年度（2027年度）報告から「包括期」に呼称変更予定**。
- 2040年の必要病床数は、医療需要（1日あたり入院患者数）を**目標病床稼働率**で割り戻して算出。目標病床稼働率は**高度急性期79%・急性期84%・回復期89%・慢性期92.5%**（新ガイドライン公表値）。入院受療率低下等の「改革モデル」も織り込む。

**このアプリでの対応**（`region_vision` モード）:
- 「急性期拠点候補」「地域急性期」「高齢者救急」等の独自スコアリング分類（あくまで病床機能報告データのみに基づく参考分類、公式認定ではない）の定義文言を、新ガイドラインの正式な機能名（急性期拠点機能／高齢者救急・地域急性期機能）に合わせて修正。「地域急性期」「高齢者救急」は本ツールでは便宜的に2区分表示しているが、新ガイドラインでは1機能である旨を注記。
- 「2040年に向けた病床需要の方向性（試算）」を、単純な病床数への係数乗算（旧: 高度急性期×0.95等）から、**実データ（在棟延べ数）から算出した現状医療需要を新ガイドラインの目標病床稼働率で割り戻す方式**に変更（`_RV_TARGET_OCC`）。需要そのものの2040年増減方向（`_RV_DEMAND_TREND`）は全国トレンドの仮定値のままで、地域差は反映していない旨を明記。
- 医療機関機能報告（令和8年10月〜）は病院単位の新しい公式データソースになりうるが、現時点ではデータ未公表のため未対応。公表され次第、既存の独自スコアリングを公式データに置き換えることを検討する。

## 令和8年度診療報酬改定の新設区分「急性期病院Ａ/Ｂ一般入院料」

令和8年度診療報酬改定で、従来の「急性期一般入院料１〜７」を再編する形で「急性期病院Ａ一般入院料」「急性期病院Ｂ一般入院料」が新設された（2026年6-7月分の施設基準届出の新規・変更届出から反映が始まっている）。

- **病床機能報告（`ward_cache.parquet`）にはまだ存在しない**: 病床機能報告は年1回・現在は令和7年度（2025年7月時点）が最新のため、令和8年度改定後の新区分はまだデータ化されていない。令和8年度分の病床機能報告が公表されるまで、`ward_cache.parquet` 側にこの値が入ることはない。
- **施設基準届出（`shisetsu_kijun_cache.parquet`）には既にある**: 2026年6-7月分の新規・変更届出の差分反映（上記「差分（新規・変更／失効）PDFによる最新化」参照）により、`区分` 列に「急性期病院Ａ一般入院料」「急性期病院Ｂ一般入院料」（精神病棟版の「急性期病院Ａ/Ｂ精神病棟１０/１３/１５対１入院基本料」含む）が全国560件収録済み。
- **条件検索「施設基準届出」タブの区分フィルター**（`app.py` の `_NYUIN_KUBUN_OPTIONS`）は病床機能報告データのみを参照する設計だったため、新区分が選択肢にも絞り込み結果にも出てこない不具合があった（2026年7月発覚）。区分フィルターの判定を病床機能報告と施設基準届出の両方から補完する形に修正し、選択肢にも追加した。病院詳細ページの「入院基本料の区分」バッジは施設基準届出の区分列を直接参照する設計だったため、この不具合の影響を受けていなかった。
- 同様の「新設区分が病床機能報告に無い」問題は、今後の診療報酬改定・病床機能報告区分変更のたびに再発しうる。新しい区分名が施設基準届出データに出現したら、`_NYUIN_KUBUN_OPTIONS` への追加を検討すること。

## 未実装機能

- **移動時間フィルター**: 住所入力 → OSRM Table API → N分以内の病院を絞り込み（SPEC.md 参照）
- **人口10万人あたりの指標**: 地域ごとの医師数・病床数を人口10万人あたりで表示。人口データは総務省「住民基本台帳に基づく人口」または国勢調査を使用すること（独自推計禁止）
- **標榜科目（診療科）データ**: 医療情報ネット オープンデータ（e-Gov ZIP）のCSV構成を要確認。座標以外のCSVに診療科目情報が含まれている可能性が高い
- **施設基準届出の全国区分カバレッジ**: 四国4県は2026年7月に正しい形式のファイルが入手できて解決済み。残る課題は関東信越・近畿・北海道・中国の精神病棟・有床診療所・結核病棟の区分（病床機能報告でカバーできない）で、対応する詳細版ファイルが厚生局から公表されていないか要確認
- **医療機関機能報告データの取り込み**: 令和8年10月開始予定。公表され次第、`region_vision` モードの独自スコアリングを公式データベースの機能区分に置き換える
- **病床機能報告「回復期」→「包括期」への呼称変更**: 令和9年度報告から適用予定。`BED_TYPES`（`data_processor.py`）や表示文言の更新は、実際に令和9年度データが報告区分名を変えて配布された時点で対応する（現時点の生データはまだ「回復期」のまま）

## 開発上の教訓・注意点（トラブルシューティング用）

- **DPCと病床機能報告は必ず同年度で表示する**: 病院詳細ページのDPCセクション（DPC患者件数・DPC分析タブ）は、選択中の病床機能報告の年度（`year`）と同じ年度のDPCのみを表示する（`_dpc_h_all[年度==year]` で判定し、無ければ `_is_dpc=False` で非表示にし他年度がある旨を案内）。2つのデータが違う年度だと誤解を招くため。現状DPCは**令和4〜6年度（2022〜2024）の3年分**（`build_dpc_append.py` で追記）。DPC公開データは各ファイルの `R0x全体`/`令和x年度` シートで年度を検証すること（病床機能報告と同様、ファイルIDやフォルダ名を信用しない）。DPC公開データは1年分で80〜90ファイルあり、`build_dpc.py` の `detect_file_type()` がシート名で6種別（施設概要表/施設別MDC別比率/高度医療/件数/MDC01-18/再入院）を判別する。「高度医療」（手術・化学療法・放射線・全身麻酔の施設別実施件数）だけ取りこぼしやすいので要確認。
- **`*`（全角＊含む）のマスク値**: 病床機能報告・様式2（手術）・DPCデータで年間10件以下の値は `*` として報告される。`pd.to_numeric(errors="coerce").fillna(0)` で単純変換すると `*` が `0` に化けて「実績なし」と誤表示される。`-1` をセンチネル値として保持し、表示時のみ `"*"` に変換すること（`data_processor.py` の手術データ、`build_dpc.py` のDPCデータで対応済み）。集計（`.sum()`）する際も `-1` 同士を単純加算すると意味不明な負の値になるため、専用の集計関数（正の値のみ合計、無ければ`-1`を維持）が必要。
- **Streamlitで `st.markdown()` 内の `<script>` は実行されない**（innerHTML経由のため）。カスタムJSを動かす場合は `st.components.v1.html()` を使い、親ページのDOMを触るなら `window.parent.document` 経由でアクセスする（ブラウザ自動翻訳の抑止用notranslateタグ設定で対応）。
- **CSSを書いたら「セレクタが実際に何件マッチしているか」を実DOMで数えること**（2026年7月、条件検索の見た目を整理する際に2件同時発覚）。いずれも「CSSは書かれているのに一度も効いていなかった」類のバグで、ソースを読むだけでは絶対に気づけない。
  - `div[data-testid="stTabPanel"]` は**Streamlitに存在しないtestid**で、マッチ0件だった（正しくは `[role="tabpanel"]`）。タブパネルの枠線・白背景・角丸が一度も適用されず、フォルダ型タブが乗る土台がないまま浮いていた。**Streamlitのtestidは公式APIではなくバージョンで変わるため、`data-testid` でスタイルを当てたら必ずPlaywrightで `locator(sel).count()` を確認する**（`role` 属性やbaseweb属性の方が安定する場合はそちらを使う）。
  - **詳細度の衝突でモバイル用CSSが打ち消されていた**: グローバルCSSの `@media (max-width:768px)` 内 `.stTabs [role="tab"]`（詳細度0,2,0）によるフォント・余白の縮小が、後から注入した `div[data-testid="stTabs"] button[data-baseweb="tab"]`（0,2,2）に負けて効かず、375px幅でタブが縮まないままだった（`!important` は両方付いているので勝敗は詳細度で決まる）。**部分的なCSSを後から注入する時は、既存の `@media` 指定を上書きしていないか実機幅で `getComputedStyle` を確認し、必要なら同じブロック内に同じ詳細度で `@media` を再宣言して復元する。**
- **入力欄のグループは「囲い」で示す。見出しの色や細い縦バーだけでは不足**（2026年7月、見出しの背景塗りを外した直後に「検索項目が分かりにくくなった」と指摘されて判明）。塗りを当てるのは**見出しではなく中身**が正しい（見出しの背景塗りはラベルを囲うだけで、どこまでが1グループかを示さない）。条件検索では他ページと同じ `st.container(border=True, key="..._filter_box")`＋`.st-key-*` のCSS（ブランド緑の淡背景パネル）でエリア関連5項目を囲って解決した。**新しい検索条件を追加する時は、必ずこのパネルの中に入れること。**
  - `st.form()` の既定の枠線は、論理的にひとまとまりでない範囲（病院名〜全タブ〜検索ボタン）を囲ってグループの目印として誤解を招くため `border=False` にし、囲いは意味のある単位ごとに明示的に付ける。
  - **都道府県→二次医療圏のカスケードを即時反映させるためエリアの3項目だけフォームの外に出す必要があり、囲いが2つ（`s_area_box` / `s_scope_box`）に分かれる。** 1枚のパネルに見せるには、上下の角丸と接する側のボーダーを落とした上で、**上側の箱に `margin-bottom: -16px` を当てる**。Streamlitの縦方向の間隔は子要素のマージンではなく親 `stVerticalBlock` の flex `gap` なので、子に `margin: 0` を指定しても隙間は消えない（実測16px残る）。
- **見出しに番号（①②…）を振るのは、本当に順番にやる必要がある場合だけにする**。条件検索は以前「① エリアを絞り込む」「② 絞り込み条件を選ぶ」という番号付きの色付きボックスだったが、実際は**どちらも省略可で相互依存もない**（①の補足自身が「すべて省略で全国対象」と書いていて番号と矛盾していた）。番号を外してアプリ共通の `.section-header`（緑の左バーのみ・背景なし）に統一し、併せて重複していた `margin-top:32px` の空div と `st.divider()` を削除した（`.section-header` が上マージン30pxを持つため合計100px近い空白帯ができていた）。実測で空白84px減・フォーム全体67px短縮。**新しいセクションを追加する時は色付きボックスを自作せず `.section-header` を使うこと。**
- **配色は必ず `:root` のトークン（`--ink` / `--line` / `--brand` 等）を使い、16進数を直書きしないこと**。app.pyは暖色寄りのトークン（`--line: #E8E4DB`、`--ink-muted: #6E6A5E`）を定義しているが、後から書き足した箇所にTailwind既定の**寒色**グレー（`#e5e7eb` / `#6b7280` / `#1f2937` 等）が直書きで混入し、同じ画面で2系統の灰色が並んで「ゴタついた」印象になっていた（2026年7月に条件検索セクションを修正、app.py全体では他にも約40箇所残存＝パンくず・病院詳細ヘッダー等、順次移行する）。装飾目的で意味色（橙・赤）を使うのも禁止（旧「②絞り込み条件を選ぶ」見出しが琥珀色で、CSSコメントに書かれた自分ルール「色はブランド緑＝操作・強調／意味色＝状態のみ」に反していた）。
- **ブラウザの自動翻訳が日本語ページを誤って再翻訳する**ことがある（「地域包括ケア」→「地域含むケア」等）。`<meta name="google" content="notranslate">` と `<html lang="ja">` を設定して防止する。
- **ユーザーのWindows環境でのgit push失敗**: ローカルブランチ名が作業指定ブランチ名と一致していないと `git push origin <branch>` が `src refspec ... does not match any` で失敗する。`git push origin HEAD:<branch>` を使うか、先に `git pull origin <branch>` でマージしてから push する。マージコミット時にVimが開いたら `Esc` → `:wq` で保存終了。
- **ローカルとmaster本番の乖離・`main`ブランチ名の混乱について**は根本原因を特定し恒久対応済み。詳細は「Git・デプロイ」セクション参照（ローカルブランチ名を`master`に統一し`origin/master`を追跡するよう設定、作業前は必ず`git pull`から）。
- **Streamlit Community Cloudのメモリ制限**（約1GB）: 大きいparquetを `@st.cache_data` 無しで読み込むとOOMでアプリが落ちる。新しいキャッシュ読み込み関数を追加する際は必ず `@st.cache_data(show_spinner=False)` を付け、可能なら `drop_duplicates()` で不要な重複行を削減する。
- **`drop_duplicates()`だけでは不十分、テキスト列のcategory化も必須**（2026年7月、施設基準届出の差分マージ後にアプリ全体が「かなり遅い」と報告され発覚）: `shisetsu_kijun_cache.parquet`（96万行）を`_load_shisetsu_kijun()`で読み込む際、都道府県コード等の一部列しかcategory化しておらず、`医療機関名称`・`住所`等の高重複率テキスト列が素の文字列のまま保持されていたため、メモリ上で240MB超（行数の8〜10倍以上、重複が非常に多い列）を占めていた。この1つのDataFrameだけでStreamlit Cloudの約1GB制限のかなりの部分を圧迫し、施設基準届出を使う検索・病院詳細ページの両方が全体的に遅くなっていた。該当列（ユニーク数が行数よりずっと少ない列すべて）をcategory化したところ実測62MB（74%減）に圧縮できた。**新しいparquetを読み込むキャッシュ関数を書く／既存データが大きく育った時は、`df[col].nunique()` と `df[col].memory_usage(deep=True)` を確認し、ユニーク数が行数よりずっと少ない列（体感目安: ユニーク数が行数の30%未満）は`category`型に変換すること。** 逆に対象データフレームが小さい（数千行程度）場合はこの最適化の効果は薄いので、大きく育ったデータセット（数万行〜）に絞って対応すれば十分。
- **`detect_file_type()`のシート内容による判定は文字列部分一致でなく完全一致にする**（実際に事故った）: DPC公開データの「再入院」ファイル判定が、シート内容に「再入院」という文字列が含まれるかどうかの部分一致だったため、無関係な表（「予定救急医療入院」のMDC別内訳、「様式１の提出件数と転棟率」等）まで再入院ファイルとして誤って取り込んでいた。これらの表は列位置が違うため、`load_readmission()` が人数（件数）をそのまま再入院率としてパースし、`再入院率`列に最大2640%等の物理的にありえない値が混入していた（2026年7月発覚、`build_dpc_append.py`／旧`build_dpc.py`いずれの取り込み経路でも同じ不具合）。ヘッダー行（1行目）の列名が「再入院率」「再転棟率」と完全一致するかで判定するよう修正。同種のキーワード部分一致による種別判定を追加する際は、ヘッダー列名の完全一致や表のセル構造（何列目に何があるか）まで確認すること。
- **医療機関名は年度によって表記が変わることがあり、`医療機関名`の完全一致で年度をまたいだ照合をしてはいけない**（2026年7月、「海老名総合病院の2025年以外のデータがない」と報告され発覚）: 病床機能報告の生データは病院が毎年自己申告する形式のため、同一施設（`医療機関コード`は不変）でも年度によって法人名の表記が変わることがある（実例: 海老名総合病院は2022〜2024年度が半角カタカナ「ｼﾞｬﾊﾟﾝﾒﾃﾞｨｶﾙｱﾗｲｱﾝｽ」、2025年度が全角「ジャパンメディカルアライアンス」で別文字列だった。全国で414施設がこの種の表記ゆれを持つ）。病院詳細ページは年度セレクタで同じ病院の別年度を表示する設計だが、`医療機関名 == hospital` で年度別データを絞り込んでいた箇所（`hosp_row`本体・地域内順位・スタッフ分析・病床/手術分析・地図・手術シェアグラフ等、計8箇所）が表記ゆれのある病院では他年度のデータを見つけられず「データなし」になっていた（`hospital_trend()`のみ元々`医療機関コード`で照合していたため経年トレンドタブだけは無事だった）。`_match_this_hospital()`ヘルパー（医療機関コードがあればコードで、無ければ選択中年度の実表記名で照合）を導入して修正。**年度をまたいで同一施設を照合する処理は、名称ではなく`医療機関コード`をキーにすること。** 新しいタブ・集計を追加する際、`df["医療機関名"] == hospital`のような比較を書きそうになったら要注意。
